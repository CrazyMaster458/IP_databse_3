<form method="post">
    <h2>Change Login</h2>
    <?php if(isset($_GET['error'])) ?>

    <div class="form-group">
        <label for="name">Jméno</label>
        <input type="text" class="form-control" id="name" name="name" value="{{employee.name}}" required>
        {{#errors.name}}<div class="d-block invalid-feedback">{{errors.name}}</div>{{/errors.name}}
    </div>
    <div class="form-group">
        <label for="name">Staré heslo</label>
        <input type="password" class="form-control" id="oldPass" name="oldPass" value="{{employee.oldPass}}" required>
        {{#errors.oldPass}}<div class="d-block invalid-feedback">{{errors.oldPass}}</div>{{/errors.oldPass}}
    </div>
    <div class="form-group">
        <label for="name">Nové heslo</label>
        <input type="password" class="form-control" id="password" name="password" value="{{employee.password}}" required>
        {{#errors.password}}<div class="d-block invalid-feedback">{{errors.password}}</div>{{/errors.password}}
    </div>
    <button type="submit" class="btn btn-primary mt-3">Change</button>
</form>